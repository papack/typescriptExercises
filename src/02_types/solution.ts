/*
 * 1.
 * export and create a variable of type boolean with the name myBoolean and the
 *  value "true".
 */

export let myBoolean: boolean = true;

/*
 * 2.
 * export and create a variable of type number with the name myNumber and the
 *  value 42.
 */

export let myNumber: number = 42;

/*
 * 3.
 * export and create a variable of type string with the name myString and the
 *  value "Lorem ipsum".
 */

export let myString: string = "Lorem ipsum";

/*
 * 4.
 * export and create a Array of numbers with the name myArray and the
 *  values 1,2,3,4
 */

export let myArray: number[] = [1, 2, 3, 4];

//Alternativ:
//  export let myArray: Array<number> = [1, 2, 3, 4];
